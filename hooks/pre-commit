#!/usr/bin/env bash
#
# Versioned git pre-commit hook, installed via hooks/install.sh
# (git config core.hooksPath hooks). Runs the repository linter scoped to
# the files staged for this commit.

set -u
exec "$(cd "$(dirname "${0}")" && pwd)/lint.sh" --staged
